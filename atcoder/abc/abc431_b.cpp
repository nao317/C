#include <iostream>
#include <vector>

using namespace std;

int main() {
    int x, n;
    cin >> x >> n;
    vector<int> w(n);
    for (int i = 0; i < n; i++) cin >> w.at(i);
    int q;
    cin >> q;
    

}